// TODO: Replace with real API calls
export const sendMessage = async (recipientId, text) => {
  // return axios.post('/api/messages', { recipientId, text });
};

export const fetchMessages = async (recipientId) => {
  // return axios.get(`/api/messages/${recipientId}`);
};
